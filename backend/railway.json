{
  "build": {
    "command": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "command": "python manage.py migrate --noinput && gunicorn config.wsgi --bind 0.0.0.0:8000",
    "healthcheckPath": "/health/"
  }
}
